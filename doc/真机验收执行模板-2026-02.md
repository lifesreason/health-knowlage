# 真机验收执行模板（2026-02）

## 1. 执行信息
- 执行日期：
- 执行人：
- 小程序版本：
- 后端分支/提交：
- 前端分支/提交：

## 2. 设备与环境
| 设备 | 系统版本 | 微信版本 | 网络 | 结果 |
| --- | --- | --- | --- | --- |
| iPhone（示例） | iOS 17.x | 8.0.x | WiFi/4G | 待测 |
| Android（示例） | Android 12 | 8.0.x | WiFi/4G | 待测 |

## 3. 执行结果总览
| 总用例数 | 通过 | 失败 | 阻塞 | 通过率 |
| --- | --- | --- | --- | --- |
| 19 | 0 | 0 | 0 | 0% |

## 4. 回归清单（逐项打勾）

### 4.1 认证与回退
- [ ] `REG-AUTH-01` 未绑定手机号用户登录后进入绑定页
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-AUTH-02` 已绑定手机号用户登录后直接返回
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-AUTH-03` 无页面栈时登录/绑定成功回退兜底
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞

### 4.2 视频与分享链路
- [ ] `REG-VIDEO-01` 视频分享打开目标视频
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-VIDEO-02` 目标视频不在首屏列表时兜底加载
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-VIDEO-03` 视频点赞动画与计数一致
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞

### 4.3 首页/搜索跳转
- [ ] `REG-FEED-01` 首页视频卡跳视频页
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-SEARCH-01` 搜索视频结果跳视频页
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-SEARCH-02` 搜索词 trim 与回退兜底
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞

### 4.4 发布与圈子
- [ ] `REG-PUB-01` 无可发布圈子时阻断发布
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-PUB-02` 带 circleId 进入发布页自动选中圈子
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-CIRCLE-01` 圈子已加入状态正确
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-CIRCLE-02` 加入/退出圈子成员数同步
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞

### 4.5 个人中心与数据一致性
- [ ] `REG-ME-01` 退出登录后我的统计重置
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞

### 4.6 视觉一致性
- [ ] `REG-UI-01` 关键页面图标风格统一
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞

### 4.7 触控热区专项
- [ ] `REG-TOUCH-01` 关闭/清除类图标热区校验
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-TOUCH-02` 列表箭头类控件热区校验
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-TOUCH-03` 删除按钮热区校验（发布器）
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞
- [ ] `REG-TOUCH-04` 关注/加入按钮最小热区校验
  - 实际结果：
  - 结论：通过 / 失败 / 阻塞

## 5. 缺陷记录
| 缺陷ID | 对应用例 | 严重级别 | 现象描述 | 复现步骤 | 附件 |
| --- | --- | --- | --- | --- | --- |
|  |  | P0/P1/P2 |  |  | 日志/截图/录屏 |

## 6. 验收结论
- 版本结论：通过 / 有条件通过 / 不通过
- 风险说明：
- 建议发布策略：
