这是一个非常详尽的**研发需求文档 (R&D Requirements Document)**。

这份文档将**产品需求 (PRD)** 转化为**技术执行语言**，明确了“怎么做”、“用什么做”以及“做到什么标准”。它是开发人员编写代码、数据库设计和接口定义的直接依据。

---

# 研发需求文档 (R&D Specification)

| 项目名称 | **银龄健康 (SilverHealth)** |
| --- | --- |
| **文档版本** | V1.0.0 (Release Candidate) |
| **对应 PRD** | V1.1 (修正版) |
| **面向对象** | 后端开发、前端开发、测试工程师、运维 |
| **技术栈** | **Frontend**: Uni-app (Vue3) / **Backend**: Node.js (NestJS) |

---

## 1. 总体技术架构 (Technical Architecture)

### 1.1 架构拓扑

采用 **前后端分离 + 微服务模块化** 架构。

* **客户端**: 微信小程序 (Uni-app 编译)，分包加载。
* **接入层**: Nginx (反向代理 / SSL / 静态资源缓存)。
* **应用层**: Node.js (NestJS Framework)，无状态服务，支持水平扩展。
* **数据层**:
* **MySQL 8.0**: 核心业务数据（用户、内容、关系）。
* **Redis 6.0**: 缓存（Session、验证码、Feed流热数据）。
* **OSS (对象存储)**: 存储图片、视频文件，配置 CDN 加速。


* **第三方服务**: 微信 API (登录/支付)、阿里云/腾讯云 (短信/内容安全)。

### 1.2 目录结构规范

#### 1.2.1 前端 (Uni-app)

```text
/src
  /api          # 接口定义 (UserApi.js, FeedApi.js)
  /common       # 公共工具 (http.interceptor.js, utils.js)
  /components   # 全局组件 (u-feed-card, u-font-scaler)
  /store        # Pinia 状态 (user, theme)
  /pages        # 主包 (Home, Video, Me)
  /sub_pkg_A    # 分包A (发布器, 圈子详情)
  /sub_pkg_B    # 分包B (个人设置, 认证)

```

#### 1.2.2 后端 (NestJS)

```text
/src
  /common       # 过滤器, 守卫, 拦截器
  /config       # 环境变量配置
  /modules
    /auth       # 认证模块
    /user       # 用户模块
    /content    # 内容模块 (Post, Comment)
    /community  # 圈子模块
    /audit      # 审核模块 (对接三方API)
  /entities     # TypeORM 实体

```

---

## 2. 核心模块研发需求 (Module Specifications)

### 2.1 认证与用户模块 (Auth & User)

* **研发目标**: 实现“静默登录 + 强制实名”的漏斗机制。
* **技术逻辑**:
1. **JWT 机制**: 登录成功返回 `access_token` (7天) 和 `refresh_token`。所有非公开接口需通过 `JwtAuthGuard`。
2. **数据加密**:
* `mobile`: 使用 **AES-256-CBC** 加密存储，密钥存放在环境变量，数据库中不可见明文。
* `password` (若有): 使用 **Bcrypt** 加盐哈希。


3. **适老化配置**: 在 `users` 表增加 `font_scale` 字段 (Default: 1.0)。前端启动时拉取该配置并应用到 CSS Variables。



### 2.2 内容发布与审核系统 (Content & Audit)

* **研发目标**: 确保所有 UGC 内容经过“机审+人审”闭环。
* **状态机设计 (State Machine)**:
* `0 (PENDING)`: 待审核 (仅作者可见)。
* `1 (PUBLISHED)`: 已发布 (公开可见)。
* `2 (REJECTED)`: 驳回 (作者可见，需附带 `reject_reason`)。
* `-1 (DELETED)`: 用户删除 (软删除)。


* **发布流程**:
1. 前端直传 OSS，获取 URL。
2. 后端接收 Post 数据，写入 DB (`status=0` 审核中)。
3. **人工审核**: 内容进入后台管理待审核列表，由管理员手动审核。
* 通过 -> 更新 `status=1` (已发布)。
* 驳回 -> 更新 `status=2` (驳回)，填写驳回原因供作者查看。
* 删除 -> 更新 `status=-1` (软删除)。
4. **API审核设计（暂不实现）**: 已设计好异步队列架构和第三方API对接逻辑，保留在代码中，后续可通过配置开关快速启用机审功能，实现"机初审+人复审"的升级流程。





### 2.3 首页推荐流 (Home Feed)

* **研发目标**: 高性能的混合内容流分发。
* **算法逻辑**:
* **SQL 基础查询**:
```sql
SELECT * FROM posts 
WHERE status = 1 
AND user_id NOT IN (blacklist_ids)
ORDER BY (created_at > NOW() - INTERVAL 3 DAY) DESC, -- 优先3天内
         (view_count + like_count * 5) DESC -- 热度降权
LIMIT 10 OFFSET N

```


* **缓存策略**: 首页前 5 页数据缓存至 Redis `feed:recommend:cache`，TTL 10分钟。


* **前端优化**: 使用 `u-list` 或 `virtual-list` 组件，防止长列表 DOM 节点过多导致低端机卡顿。

### 2.4 视频沉浸流 (Video Player)

* **研发目标**: 仿抖音交互，流畅播放。
* **技术实现**:
* 组件：使用 Uni-app 原生 `<video>` 组件（层级最高，注意覆盖问题）。
* **预加载**: 监听 `swiper` 的 `change` 事件。当滑到 Index=1 时，静默加载 Index=2 的视频资源。
* **流量保护**: 监听 `uni.getNetworkType`。非 Wifi 环境且未授权流量播放时，自动暂停并弹窗。



---

## 3. 数据库设计 (Database Schema)

### 3.1 核心表结构 (MySQL)

**用户表 (`sys_user`)**
| 字段 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| id | BIGINT | PK, AI | 用户ID |
| openid | VARCHAR(64) | UK | 微信 OpenID |
| mobile_cipher | VARCHAR(128)| | 加密后的手机号 |
| role | TINYINT | Default 0 | 0:普通用户, 1:认证医师, 9:管理员 |
| settings_json| JSON | | 存储字体倍率等偏好 |

**帖子表 (`biz_post`)**
| 字段 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| id | BIGINT | PK | 分布式ID (Snowflake) |
| user_id | BIGINT | Index | 作者ID |
| circle_id | BIGINT | Index | 所属圈子ID |
| type | TINYINT | | 1:图文, 2:视频 |
| content | TEXT | | 正文 (富文本 HTML) |
| media_urls | JSON | | 图片/视频地址列表 |
| audit_status | TINYINT | Index | 0:审核中, 1:通过, 2:驳回 |

**互动表 (`biz_interaction`)**

* 设计为一张宽表或分表 (`likes`, `comments`)，这里建议分表以提升性能。
* 关键字段: `target_id`, `target_type` (post/comment), `from_user_id`.

---

## 4. 接口规范 (API Specification)

**Base URL**: `https://api.silverhealth.com/v1`

### 4.1 响应包装 (Response Wrapper)

```typescript
interface ApiResponse<T> {
  code: number;      // 0:成功, >0: 业务错误
  msg: string;       // 提示信息
  data: T | null;    // 业务数据
  timestamp: number; // 毫秒时间戳
}

```

### 4.2 关键接口定义

| 模块 | 方法 | URI | 描述 | 参数示例 |
| --- | --- | --- | --- | --- |
| **Auth** | POST | `/auth/login` | 微信登录 | `{ code: "wx_code" }` |
| **Auth** | POST | `/auth/bind` | 绑定手机 | `{ encryptedData, iv }` |
| **Feed** | GET | `/feed/list` | 获取流 | `?type=recommend&page=1` |
| **Post** | POST | `/post/publish` | 发布内容 | `{ type: 1, content: "...", circleId: 101 }` |
| **Circle** | POST | `/circle/join` | 加入圈子 | `{ circleId: 101 }` |
| **Upload** | GET | `/oss/policy` | 获取上传凭证 | `?fileType=image` (前端直传用) |

---

## 5. 非功能性需求 (NFR)

### 5.1 性能要求

* **API 响应时间**: 95% 的请求需在 **200ms** 内返回（不含网络传输）。
* **首屏渲染 (FCP)**: 小程序冷启动耗时 < **1.5s**。
* **长列表性能**: Android 低端机 (骁龙6系) 滑动帧率 > **45 FPS**。

### 5.2 安全要求

* **SQL 注入**: 严禁拼接 SQL，必须使用 TypeORM Repository API。
* **XSS 防御**: 富文本展示端使用 `mp-html` 组件，并配置白名单过滤。
* **鉴权**: 所有写操作接口 (POST/PUT/DELETE) 必须校验 Token。

### 5.3 适老化特殊要求

* **点击热区**: CSS 中所有 `Clickable` 元素增加 `padding: 10px` 或设置 `min-width/height: 44px`。
* **字体缩放**: 必须支持 CSS Variable 动态调整，最大支持 1.4 倍放大，且布局不崩坏（使用 Flex/Grid 布局，避免固定高度）。

---

## 6. 研发交付物 (Deliverables)

1. **源代码**: 托管于 GitLab/GitHub，包含前端、后端。
2. **SQL 脚本**: `init.sql` (建表) 和 `seed.sql` (初始字典数据、测试圈子数据)。
3. **API 文档**: 实时更新的 Swagger/YApi 地址。
4. **测试报告**: 包含单元测试覆盖率报告 (后端核心模块 > 80%) 和 压力测试报告。
5. **部署文档**: Dockerfile 及 Kubernetes/PM2 部署配置。

---

## 附录A：实现对齐更新（2026-02-12）

以下内容用于将研发需求与当前代码实现对齐：

1. 认证与短信
- 已实现 `session_key`、短信验证码写入 Redis（含内存兜底），键名采用：
  - `sh:auth:wx_session:{userId}`
  - `sh:auth:sms:{mobile}`
- 已实现短信网关抽象与多 Provider 适配：
  - `SMS_PROVIDER=mock|aliyun|tencent`
  - 阿里云、腾讯云 Provider 均已落地，运行时按配置切换。
- 已增加启动期短信配置校验（Provider 合法性与必填项），避免运行时才暴露配置问题。

2. Feed 与互动
- 首页 `feed` 三类流已区分：
  - `recommend`：时间+互动热度综合排序
  - `follow`：按关注关系过滤（需登录）
  - `nearby`：支持经纬度入参并按距离近似排序
- 已实现关注关系能力：
  - `POST /interaction/follow`
  - `GET /interaction/following`
  - `GET /interaction/followers`
- 已实现收藏真实落库与计数同步，不再是前端假状态。

3. 用户侧能力
- 已实现用户统计、收藏、浏览历史接口：
  - `GET /user/my-stats`
  - `GET /user/collections`
  - `POST /user/history`
  - `GET /user/history`

4. 错误语义
- 核心业务模块已将 `throw new Error` 收敛为标准 HTTP 异常（`BadRequestException` / `NotFoundException`），便于前端按状态码处理。

5. 联调增强
- 已新增认证续期接口：`POST /auth/refresh`。
- 已新增机审运行态接口（管理员）：
  - `GET /audit/machine/status`
  - `POST /audit/machine/run-once`

6. 缓存可观测性
- 已新增推荐流缓存命中统计接口（管理员）：
  - `GET /stats/feed-cache`
- 支持分钟窗口参数：
  - `windowMinutes`（1~180，默认10）
- 统计项包含：总请求数、命中数、未命中数、命中率、Redis 命中、内存命中、Redis 错误数、内存缓存条目数、TTL 配置。
- 响应结构：
  - `total`：进程启动以来累计统计
  - `window`：最近 N 分钟窗口汇总 + 每分钟序列

7. 分享能力
- 已新增小程序码接口：
  - `GET /share/wxacode`
- 典型参数：
  - `scene`（必填，建议 URL 编码）
  - `page`（选填，默认 `pages/index/index`）
  - `width`（选填，默认 `280`）
- 返回：`image/png` 二进制流（可直接用于海报生成）。

## 附录B：机审/分享接口对齐（2026-02-12）

### 1) 机审运行态（Admin）

1. `GET /api/v1/audit/machine/status`
- 鉴权：`JWT + Admin(roles=9)`
- 用途：查看机审队列长度、处理器状态。

2. `POST /api/v1/audit/machine/run-once`
- 鉴权：`JWT + Admin(roles=9)`
- 用途：手动消费一条机审任务，便于联调与应急处理。

### 2) 分享小程序码（Public）

1. `GET /api/v1/share/wxacode`
- 鉴权：无需登录（`@Public`）
- Query:
  - `scene: string` 必填
  - `page?: string` 选填
  - `width?: number` 选填
- 响应：
  - `Content-Type: image/png`
  - `Cache-Control: public, max-age=300`
