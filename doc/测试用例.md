这是一份基于 **PRD V1.1** 和 **详细设计文档** 编写的**系统测试用例 (System Test Cases)**。

这份文档覆盖了核心业务流程、异常场景以及针对“银发族”的特殊体验测试，可供 QA 团队直接导入 **Jira / 禅道 / Excel** 进行执行。

---

# 测试用例清单 - 银龄健康 (SilverHealth)

| **项目名称** | 银龄健康小程序 | **测试阶段** | 系统集成测试 (SIT) |
| --- | --- | --- | --- |
| **覆盖版本** | V1.0.0 | **优先级定义** | P0-阻断性, P1-核心功能, P2-一般错误 |

---

## 1. 核心功能测试 (Functional Testing)

### 1.1 认证与权限 (Auth)

**测试目标**：验证“静默登录+强制实名”的漏斗机制是否生效。

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **TC-001** | 游客模式浏览 | 未登录 | 1. 打开小程序。<br>

<br>2. 浏览首页推荐流。<br>

<br>3. 点击进入文章详情。<br>

<br>4. 观看视频。 | 所有浏览行为均正常，**不弹出**登录框。 | P0 |
| **TC-002** | 互动触发登录(未注册) | 未登录 | 1. 在视频播放页双击点赞。<br>

<br>2. 弹出授权弹窗，点击“微信一键登录”。<br>

<br>3. 同意获取手机号。 | 1. 登录成功，提示“登录成功”。<br>

<br>2. **自动完成刚才的点赞操作** (红心高亮)。<br>

<br>3. 数据落库 `sys_user` 表。 | P0 |
| **TC-003** | 拒绝手机号授权 | 未登录 | 1. 点击“加入圈子”。<br>

<br>2. 弹窗点击“微信登录”。<br>

<br>3. 在微信原生授权页点击“拒绝”。 | 1. 回到当前页。<br>

<br>2. Toast 提示“依据法规互动需实名”。<br>

<br>3. 加入圈子操作未生效。 | P1 |
| **TC-004** | 退出登录 | 已登录 | 1. 进入“我的-设置”。<br>

<br>2. 点击“退出登录”。 | 1. 清除本地 Token。<br>

<br>2. 跳转回首页。<br>

<br>3. 再次点赞时重新弹出登录框。 | P2 |

### 1.2 适老化适配 (Elderly UI)

**测试目标**：验证 CSS 变量切换逻辑及大字模式下的布局稳定性。

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **TC-005** | 设置字体倍率(1.2x) | 任意状态 | 1. 进入“我的-设置”。<br>

<br>2. 拖动字体滑块至“大”。<br>

<br>3. 返回首页/文章页查看。 | 1. 全局文字明显变大。<br>

<br>2. 标题未被截断。<br>

<br>3. 底部 TabBar 文字未溢出。 | P0 |
| **TC-006** | 极大字体适配(1.4x) | 任意状态 | 1. 拖动滑块至“特大”。<br>

<br>2. 进入“圈子广场”。<br>

<br>3. 查看密集排版的列表。 | 1. 布局未崩坏（无重叠、错位）。<br>

<br>2. 按钮点击热区正常。<br>

<br>3. 文章详情页图片宽度自适应。 | P1 |
| **TC-007** | 悬浮窗调节字体 | 阅读文章中 | 1. 点击右下角“A”悬浮球。<br>

<br>2. 调整滑块。 | 仅当前文章页字体实时变化，不卡顿。 | P2 |

### 1.3 内容发布与审核 (Publisher & Audit)

**测试目标**：验证 UGC 内容的上传、状态机流转及审核闭环。

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **TC-008** | 发布图文(正常) | 已登录 | 1. 点击发布，选择图文。<br>

<br>2. 上传 9 张图片，输入合法文字。<br>

<br>3. 选择圈子，点击发布。 | 1. 提示“发布成功，审核中”。<br>

<br>2. 跳转回圈子详情页。<br>

<br>3. 列表暂不显示该贴（除作者本人）。 | P0 |
| **TC-009** | 发布视频(超限) | 已登录 | 1. 点击发布，选择视频。<br>

<br>2. 选择一个 >200MB 或 >3分钟的视频。 | 前端直接拦截，提示“视频大小/时长超出限制”。 | P1 |
| **TC-010** | 内容含敏感词 | 已登录 | 1. 标题输入含违禁词（如涉政/脏话）。<br>

<br>2. 点击发布。 | 1. 接口返回拦截。<br>

<br>2. 前端提示“内容包含违规词，请修改”。<br>

<br>3. **不**发起 OSS 上传。 | P1 |
| **TC-011** | 机审自动通过流程 | TC-008后，`AUDIT_MACHINE_ENABLED=true`，`AUDIT_MACHINE_AUTO_PASS=true` | 1. 管理员执行 `POST /api/v1/audit/machine/run-once`。<br>

<br>2. 用户刷新首页/圈子页。 | 1. `biz_post.audit_status=1`。<br>

<br>2. 帖子在公域列表中可见。<br>

<br>3. `sys_audit_log` 写入机器审核通过记录。 | P0 |
| **TC-012** | 机审驳回/人工驳回流程 | 已发布或待审核内容存在 | 1. 设置 `AUDIT_MACHINE_AUTO_REJECT=true` 且命中拦截词，执行 `POST /api/v1/audit/machine/run-once`。<br>

<br>2. 或后台人工操作“驳回”，填写原因“图片模糊”。<br>

<br>2. 用户进入“我的-我的发布”。 | 1. 状态标签显示“未通过”。<br>

<br>2. 点击显示驳回原因弹窗。<br>

<br>3. 支持点击“重新编辑”。<br>

<br>4. `sys_audit_log` 存在对应驳回记录（机器或人工）。 | P1 |

### 1.4 社区互动 (Community)

**测试目标**：验证社交关系的建立和数据一致性。

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **TC-013** | 加入/退出圈子 | 已登录 | 1. 进入未加入的圈子，点“加入”。<br>

<br>2. 刷新页面或重进。<br>

<br>3. 点击“已加入”，确认退出。 | 1. 按钮状态即时切换。<br>

<br>2. “我的-加入的圈子”数据同步增减。<br>

<br>3. 退出后无法在该圈子发帖。 | P0 |
| **TC-014** | 双击点赞视频 | 播放视频中 | 1. 双击屏幕中心。<br>

<br>2. 再次双击。 | 1. 第一次：出现爱心动画，侧边栏红心亮起，数字+1。<br>

<br>2. 第二次：仅播放动画，数字**不**增加（幂等性）。 | P1 |
| **TC-015** | 评论发布 | 已登录 | 1. 视频页点击评论。<br>

<br>2. 输入评论发送。 | 1. 评论提交接口返回 `auditStatus=0`。<br>

<br>2. 提示“评论已提交，审核后展示”。<br>

<br>3. 评论在审核通过前不出现在公开评论列表，帖子评论数不立即增加。 | P1 |
| **TC-016** | 评论人工审核通过/驳回 | 存在待审核评论 | 1. 管理员查询 `GET /api/v1/comment/admin/list?status=0`。<br>

<br>2. 调用 `POST /api/v1/comment/admin/:id/approve` 或 `POST /api/v1/comment/admin/:id/reject`。<br>

<br>3. 用户刷新评论列表。 | 1. 通过后评论可见且帖子评论数+1。<br>

<br>2. 驳回后评论不可见，帖子评论数不增加。 | P0 |
| **TC-017** | 二级评论回复展示 | 存在至少1条已通过的一级评论 | 1. 在评论弹窗点击一级评论“回复”，输入内容发送。<br>

<br>2. 管理员审核通过该回复。<br>

<br>3. 用户在评论弹窗展开“查看回复”。 | 1. 回复内容显示在对应一级评论下方，且展示“回复 xxx”关系。<br>

<br>2. 二级评论支持点赞与继续回复。 | P1 |
| **TC-018** | 驳回内容重新发布引导 | 我的发布存在驳回内容 | 1. 进入“设置-我的发布-未通过”。<br>

<br>2. 点击一条驳回内容。<br>

<br>3. 在弹窗点击“去修改”。 | 1. 展示驳回原因弹窗。<br>

<br>2. 跳转发布器并自动带入标题、正文、类型、圈子。<br>

<br>3. 用户需重新上传素材后可再次发布。 | P1 |

---

## 2. 非功能性测试 (Non-Functional Testing)

### 2.1 性能测试 (Performance)

| ID | 测试场景 | 指标要求 | 测试方法 |
| --- | --- | --- | --- |
| **PERF-01** | 首页 Feed 流加载 | 首屏渲染时间 < 1.5s | 使用微信开发者工具 Audits 面板评分。 |
| **PERF-02** | 长列表滑动 | Android 低端机 FPS > 45 | 在红米 Note 8/9 等机型上快速滑动 50 页。 |
| **PERF-03** | 视频预加载 | 切换视频无明显黑屏 (< 200ms) | 弱网工具限制为 4G 速度，快速划动视频。 |

### 2.2 兼容性测试 (Compatibility)

| ID | 测试场景 | 覆盖范围 |
| --- | --- | --- |
| **COMP-01** | 屏幕适配 | 刘海屏 (iPhone 14), 挖孔屏 (Huawei P50), 老年机 (大额头下巴)。检查底部安全区遮挡。 |
| **COMP-02** | OS 版本 | iOS 12 ~ 17, Android 8.0 ~ 14.0。 |
| **COMP-03** | 微信版本 | 验证微信基础库最低版本 (如 2.30.0) 提示升级逻辑。 |

### 2.3 网络异常测试 (Network)

| ID | 测试场景 | 预期结果 |
| --- | --- | --- |
| **NET-01** | 断网加载首页 | 展示“网络开小差”缺省页，点击重试按钮可触发重新请求。 |
| **NET-02** | 视频播放中切网 | (Wifi -> 4G) 视频自动暂停，弹窗提示“正在使用流量”。 |
| **NET-03** | 发布中途断网 | Loading 消失，Toast 提示失败，停留在编辑页，**用户输入内容不丢失**。 |

---

## 3. 边界与数据测试 (Edge Cases)

| ID | 用例标题 | 测试步骤 | 预期结果 |
| --- | --- | --- | --- |
| **EDGE-01** | 超长文本显示 | 修改昵称为 20 个汉字，发布标题 50 个字。 | 1. 昵称在评论区/首页显示省略号。<br>

<br>2. 标题在列表最多显示 2 行，超出省略。 |
| **EDGE-02** | 空数据状态 | 1. 搜索不存在的关键词。<br>

<br>2. 进入一个新创建的空圈子。 | 1. 展示空状态插画。<br>

<br>2. 文案提示“暂无内容”。 |
| **EDGE-03** | 账号被封禁 | 1. 后台将测试账号状态设为 `0` (封禁)。<br>

<br>2. 账号尝试登录或发帖。 | 弹窗提示“账号状态异常/已封禁，请联系客服”。 |
| **EDGE-04** | 资源删除 | 1. 用户 A 收藏了 文章 B。<br>

<br>2. 文章 B 被作者删除。<br>

<br>3. 用户 A 在收藏列表点击 B。 | Toast 提示“内容已被删除”，并自动从收藏列表移除或置灰。 |

---

## 4. 测试交付物

1. **Bug 列表**: 在 Jira 中提交，包含复现步骤、截图/录屏、日志 (vConsole)。
2. **测试报告**: 包含用例执行通过率、遗留 Bug 风险评估。
3. **机型覆盖表**: 记录测试过的具体真机型号及表现。

---

**建议**: 由于本项目涉及**老年群体**，QA 在执行 `TC-005` (字体) 和 `TC-006` (布局) 时，请务必在真机上把系统字体也调大，模拟真实环境进行测试。

---

## 5. 回归专项（2026-02 本轮修复）

### 5.1 认证与回退

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **REG-AUTH-01** | 未绑定手机号用户登录后进入绑定页 | 新微信用户，数据库无手机号 | 1. 打开登录页。<br><br>2. 微信一键登录成功。 | 登录后进入手机号绑定步骤，不会直接当作“已绑定”。 | P0 |
| **REG-AUTH-02** | 已绑定手机号用户登录后直接返回 | 账号已绑定手机号 | 1. 打开登录页。<br><br>2. 微信一键登录。 | 登录成功后直接返回上一页（无绑定步骤）。 | P0 |
| **REG-AUTH-03** | 无页面栈时登录/绑定成功回退兜底 | 从外部直达 auth 页（仅 1 个页面栈） | 1. 完成登录或绑定。 | 不报错，自动回首页。 | P1 |

### 5.2 视频与分享链路

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **REG-VIDEO-01** | 视频分享打开目标视频 | 任意视频详情可分享 | 1. 分享视频 A。<br><br>2. 从分享卡片进入。 | 视频页首屏定位到视频 A。 | P0 |
| **REG-VIDEO-02** | 目标视频不在首屏列表时兜底加载 | 分享视频较旧，不在第一页 | 1. 打开分享链接。 | 目标视频可被补拉并播放，不会落到其他视频。 | P0 |
| **REG-VIDEO-03** | 视频点赞动画与计数一致 | 视频流中 | 1. 双击点赞。<br><br>2. 再次双击。 | 动画显示正常；点赞计数无异常跳变。 | P1 |

### 5.3 首页/搜索跳转

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **REG-FEED-01** | 首页视频卡跳视频页 | 首页存在 `type=2` 内容 | 点击视频卡片。 | 跳转 `/pages/video/video?id=...`。 | P0 |
| **REG-SEARCH-01** | 搜索视频结果跳视频页 | 搜索结果含视频 | 点击视频结果。 | 跳转视频页而非图文详情页。 | P0 |
| **REG-SEARCH-02** | 搜索词 trim 与回退兜底 | 从外部直达搜索页 | 1. 输入前后带空格关键词。<br><br>2. 点击取消。 | 搜索词会去空格；无历史栈时回首页。 | P1 |

### 5.4 发布与圈子

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **REG-PUB-01** | 无可发布圈子时阻断发布 | 用户未加入任何圈子 | 1. 进入发布页。<br><br>2. 尝试发布。 | 明确提示“请先加入圈子”，发布被拦截。 | P0 |
| **REG-PUB-02** | 带 circleId 进入发布页自动选中圈子 | 通过圈子详情“发帖”进入 | 进入发布页。 | 对应圈子自动选中。 | P1 |
| **REG-CIRCLE-01** | 圈子已加入状态正确 | 用户已加入该圈子 | 打开圈子详情。 | 按钮显示“已加入”。 | P0 |
| **REG-CIRCLE-02** | 加入/退出圈子成员数同步 | 可加入/退出的圈子 | 1. 点击加入。<br><br>2. 点击退出。 | 成员数本地即时 +1/-1，刷新后与后端一致。 | P1 |

### 5.5 个人中心与数据一致性

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **REG-ME-01** | 退出登录后我的统计重置 | 先登录并有统计数据 | 1. 进入我的页查看数据。<br><br>2. 退出登录。 | 统计卡清零，不显示上个账号残留数据。 | P1 |

### 5.6 视觉一致性（图标风格）

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **REG-UI-01** | 关键页面图标风格统一 | 首页/搜索/视频/发布/登录/设置 | 全链路浏览并截图对比。 | 关键按钮使用统一文本徽标风格，无明显 emoji 混杂。 | P2 |

### 5.7 机审链路回归

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **REG-AUDIT-01** | 机审运行态可观测 | 已登录管理员，机审开关开启 | 调用 `GET /api/v1/audit/machine/status`。 | 返回 `enabled/provider/batchSize/maxRetry/queue` 等关键字段。 | P0 |
| **REG-AUDIT-02** | 机审手动消费与状态回写 | 存在 `audit_status=0` 的帖子 | 调用 `POST /api/v1/audit/machine/run-once`。 | 返回包含 `processed/postId/action`，且帖子状态按规则变化并写入 `sys_audit_log`。 | P0 |
| **REG-AUDIT-03** | Provider 失败重试与复审兜底 | 配置错误的云 Provider | 1. 调用 `POST /api/v1/audit/machine/run-once` 多次触发失败。<br><br>2. 观察重试耗尽行为。 | 达到 `AUDIT_MACHINE_MAX_RETRY` 后不再重试，写入 `provider_error` 日志，帖子保留待人工复审。 | P1 |

### 5.8 触控热区与图标一致性回归

| ID | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **REG-TOUCH-01** | 关闭/清除类图标热区校验 | 进入搜索页、评论弹窗、登录弹窗、发布圈子选择弹窗 | 分别点击图标边缘区域（距图标视觉边界约 10~20rpx）触发操作。 | 即使点击在图标外围，仍可稳定触发关闭/清除行为；误触率显著降低。 | P0 |
| **REG-TOUCH-02** | 列表箭头类控件热区校验 | 进入“我的”与“设置”页 | 在右箭头周边空白处点击进入下一级页面。 | 可触发进入行为，且不会出现“必须精确点中箭头图形”问题。 | P1 |
| **REG-TOUCH-03** | 删除按钮热区校验（发布器） | 发布页已选中图片或视频 | 点击素材右上角删除按钮外围区域进行删除。 | 删除操作可触发，视觉尺寸不突兀，素材正确移除。 | P1 |
| **REG-TOUCH-04** | 关注/加入按钮最小热区校验 | 视频页、圈子详情页、文章详情页 | 点击按钮边缘区域执行关注/加入操作。 | 操作可触发，按钮状态与计数正确变化，无布局抖动。 | P1 |
