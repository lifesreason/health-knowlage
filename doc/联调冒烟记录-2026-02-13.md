# 联调冒烟记录（2026-02-13）

## 1. 执行目标
- 验证前后端在当前代码基线下的可构建性与基础启动链路。
- 记录启动阶段异常，并给出可复现/可排查信息。

## 2. 执行环境
- 工作目录：`/Volumes/gg/workspace/other/health-knowlage`
- 执行方式：命令行冒烟（非真机）
- 说明：当前执行环境存在网络与系统权限限制（沙箱）

## 3. 执行项与结果

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 后端构建 | `cd backend && npm run build` | 通过 |
| 前端小程序构建 | `cd frontend && npm run build:mp-weixin` | 通过 |
| 后端启动冒烟 | `cd backend && npm run start`（观察 15s 日志） | 启动流程进入模块初始化，但数据库连接失败重试 |

## 4. 异常场景记录

### 4.1 数据库连接失败重试
- 现象：`TypeOrmModule` 持续输出 `Unable to connect to the database. Retrying (...)`
- 日志关键字：`AggregateError [EPERM]`
- 影响：服务无法进入可用态，接口联调受阻。
- 结论：当前环境下属于网络/权限限制导致，非代码编译错误。

### 4.2 Redis 不可用降级
- 现象：`AuditQueueService` / `AuthCacheService` / `FeedCacheService` 输出 `Redis unavailable, fallback to memory ...`
- 影响：缓存与审核队列降级到内存模式，功能可继续开发但不代表生产可用状态。
- 结论：降级机制生效正常。

## 5. 建议的本地复核步骤（非沙箱）
1. 确认 MySQL 容器与 Redis 容器处于 `Up` 状态。
2. 校对 `backend/.env` 中 `DB_HOST/DB_PORT/DB_USERNAME/DB_PASSWORD/REDIS_HOST/REDIS_PORT`。
3. 执行 `cd backend && npm run start`，确认出现 `Nest application successfully started`。
4. 依次验证：登录、首页流、圈子加入/退出、发布、收藏/取消收藏、我的统计。

## 6. 本轮结论
- 代码层面：前后端构建通过。
- 运行层面：当前受限环境无法完成完整在线联调；缓存降级链路正常。
- 下一步：在本机真实网络环境执行一次完整启动+接口链路验收，并回填到真机验收模板。
