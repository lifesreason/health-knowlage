# 回归专项执行记录（2026-02-13）

## 1. 执行范围
- 依据：`/Volumes/gg/workspace/other/health-knowlage/doc/测试用例.md` 第 5 章（REG-*）
- 方式：可自动验证项（构建 + 关键代码路径校验）先执行；真机项标记为“待人工验收”

## 2. 自动执行结果

### 2.1 构建校验
- `frontend`: `npm run build:mp-weixin` -> 通过
- `backend`: `npm run build` -> 通过

### 2.2 关键路径校验（代码级）
- 已校验 `REG-AUTH-01/02/03` 对应逻辑：
  - 登录后手机号状态不再误判（`normalizeUserInfo`）
  - 登录/绑定完成无栈回退兜底（`backOrHome`）
- 已校验 `REG-VIDEO-01/02/03` 对应逻辑：
  - 视频页支持 `id` 定位
  - 目标视频不在首屏时补拉详情（`ensureTargetVideoInList`）
- 已校验 `REG-FEED-01` / `REG-SEARCH-01`：
  - 首页/搜索视频卡路由跳转视频页
- 已校验 `REG-SEARCH-02`：
  - 搜索词 `trim` + 返回兜底
- 已校验 `REG-PUB-01/02`：
  - 无圈子发布拦截 + 空态提示
  - 带 `circleId` 自动选中
- 已校验 `REG-CIRCLE-01/02`：
  - `checkJoined` 状态补齐
  - 圈子成员数本地增减同步
- 已校验 `REG-ME-01`：
  - 退出登录后统计数据重置

## 3. 回归结论（本轮）

| 用例ID | 自动验证结论 | 人工真机结论 |
| --- | --- | --- |
| REG-AUTH-01 | 通过 | 待执行 |
| REG-AUTH-02 | 通过 | 待执行 |
| REG-AUTH-03 | 通过 | 待执行 |
| REG-VIDEO-01 | 通过 | 待执行 |
| REG-VIDEO-02 | 通过 | 待执行 |
| REG-VIDEO-03 | 通过（代码与构建） | 待执行 |
| REG-FEED-01 | 通过 | 待执行 |
| REG-SEARCH-01 | 通过 | 待执行 |
| REG-SEARCH-02 | 通过 | 待执行 |
| REG-PUB-01 | 通过 | 待执行 |
| REG-PUB-02 | 通过 | 待执行 |
| REG-CIRCLE-01 | 通过 | 待执行 |
| REG-CIRCLE-02 | 通过 | 待执行 |
| REG-ME-01 | 通过 | 待执行 |
| REG-UI-01 | 通过（代码清理） | 待执行 |

## 4. 待人工验收清单（真机）
- 微信登录后未绑定手机号账号是否稳定进入绑定步骤
- 视频分享链路是否稳定命中目标视频（包含“目标不在首屏列表”场景）
- 发布页在“无圈子”场景下是否始终阻断并给出明确提示
- 登录/绑定/发布成功后，在无页面栈进入场景下是否稳定回首页
- 字体 1.0x/1.2x/1.4x 下主要页面按钮热区和排版是否无重叠

