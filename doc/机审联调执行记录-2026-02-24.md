# 机审联调执行记录（2026-02-24）

## 1. 执行结论
- 结论：通过（Mock Provider 场景）
- 执行时间：2026-02-24
- 执行方式：后端启动 + `npm run smoke:audit`

## 2. 执行命令

```bash
# 启动（临时覆盖机审与微信 mock）
AUDIT_MACHINE_ENABLED=true \
AUDIT_MACHINE_REVIEW_ENABLED=true \
AUDIT_MACHINE_PROVIDER=mock \
WX_LOGIN_MOCK_ENABLED=true \
npm run start

# 冒烟脚本
AUDIT_MACHINE_ENABLED=true \
AUDIT_MACHINE_REVIEW_ENABLED=true \
AUDIT_MACHINE_PROVIDER=mock \
WX_LOGIN_MOCK_ENABLED=true \
BASE_URL=http://localhost:3000/api/v1 \
npm run smoke:audit
```

## 3. 关键结果
1. `GET /api/v1/audit/machine/status`
- `enabled=true`
- `provider=mock`
- `batchSize=1`
- `maxRetry=2`
- `queue.total` 从 `1` 下降到 `0`

2. `POST /api/v1/audit/machine/run-once`
- 返回：`processed=true`
- 返回动作：`action=auto_approve`
- 返回决策：`decision=pass`

3. 数据落库
- `biz_post.audit_status`：`0 -> 1`
- `sys_audit_log`：新增机器审核记录
- `details` 包含：`provider=mock`、`decision=pass`、`action=auto_approve`

## 4. 执行前置修复与环境说明
- 初次执行失败原因为 `localhost:3306` 连接拒绝。
- 已确认并拉起 Docker 依赖服务（MySQL/Redis healthy）后重跑成功。

## 5. 后续待验收
1. 阿里云 Provider 真实联调（云侧开通、密钥、策略、回包落库）。
2. 腾讯云 Provider 真实联调（云侧开通、密钥、策略、回包落库）。
3. Provider 异常重试场景（达到 `AUDIT_MACHINE_MAX_RETRY` 后进入人工复审）。
